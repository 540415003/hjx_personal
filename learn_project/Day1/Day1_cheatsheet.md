# 📝 Day 1 快速参考卡片

> 随时查阅的Go语法速查表

## 变量声明

```go
// 方式1: 完整声明
var name string = "张三"

// 方式2: 类型推断
var age = 25

// 方式3: 短声明（最常用）
city := "北京"

// 方式4: 批量声明
var (
    username = "admin"
    password = "123456"
)

// 方式5: 多变量
var x, y, z int = 1, 2, 3
a, b := "Hello", "World"
```

## 基本数据类型

| 类型 | 说明 | 零值 | Java对应 |
|------|------|------|----------|
| `bool` | 布尔值 | `false` | `boolean` |
| `string` | 字符串 | `""` | `String` |
| `int` | 整数（32/64位）| `0` | `int` |
| `int8` | 8位整数 | `0` | `byte` |
| `int16` | 16位整数 | `0` | `short` |
| `int32` | 32位整数 | `0` | `int` |
| `int64` | 64位整数 | `0` | `long` |
| `uint` | 无符号整数 | `0` | - |
| `float32` | 32位浮点 | `0.0` | `float` |
| `float64` | 64位浮点 | `0.0` | `double` |
| `byte` | uint8别名 | `0` | `byte` |
| `rune` | int32别名 | `0` | `char` |

## 类型转换

```go
// ✅ 正确：显式转换
var i int = 42
var f float64 = float64(i)

// ❌ 错误：不能隐式转换
var x int = 10
var y float64 = x  // 编译错误！

// 常见转换
int64(i)        // int -> int64
float64(i)      // int -> float64
int(f)          // float64 -> int
string(r)       // rune -> string
```

## 常量

```go
// 单个常量
const Pi = 3.14159

// 批量常量
const (
    StatusOK    = 200
    StatusError = 500
)

// 使用iota（自动递增）
const (
    Sunday = iota    // 0
    Monday           // 1
    Tuesday          // 2
)

// iota高级用法
const (
    _  = iota        // 跳过0
    KB = 1 << (10 * iota)  // 1024
    MB                      // 1048576
    GB                      // 1073741824
)
```

## 格式化输出

```go
// Print系列
fmt.Print("不换行")
fmt.Println("自动换行")
fmt.Printf("格式化: %s\n", name)

// 常用占位符
%v     // 默认格式
%+v    // 带字段名的结构体
%T     // 类型
%t     // 布尔值
%d     // 十进制整数
%b     // 二进制
%o     // 八进制
%x     // 十六进制（小写）
%X     // 十六进制（大写）
%f     // 浮点数
%.2f   // 保留2位小数
%s     // 字符串
%c     // 字符
%p     // 指针
%e     // 科学计数法
```

## 数学运算

```go
// 基本运算
a + b    // 加
a - b    // 减
a * b    // 乘
a / b    // 除
a % b    // 取余

// 自增自减（只有后置）
i++      // ✅ 正确
i--      // ✅ 正确
++i      // ❌ 错误
--i      // ❌ 错误

// 复合赋值
a += b   // a = a + b
a -= b   // a = a - b
a *= b   // a = a * b
a /= b   // a = a / b
a %= b   // a = a % b

// math包常用函数
math.Abs(-5.5)        // 绝对值: 5.5
math.Ceil(5.3)        // 向上取整: 6
math.Floor(5.8)       // 向下取整: 5
math.Round(5.5)       // 四舍五入: 6
math.Sqrt(16)         // 平方根: 4
math.Pow(2, 3)        // 幂运算: 8
math.Max(10, 20)      // 最大值: 20
math.Min(10, 20)      // 最小值: 10
```

## Go vs Java 快速对比

| 特性 | Go | Java |
|------|----|----|
| 变量声明 | `name string` | `String name` |
| 类型推断 | `x := 10` | `var x = 10` (Java 10+) |
| 零值 | 自动初始化 | 成员变量有默认值 |
| 类型转换 | 必须显式 | 部分可隐式 |
| 常量 | `const Pi = 3.14` | `final double PI = 3.14` |
| 自增 | 只有后置 `i++` | 前置和后置都支持 |
| 未使用变量 | 编译错误 | 编译警告 |
| 枚举 | 用 `iota` | 用 `enum` |
| 字符 | `rune` (32位) | `char` (16位) |

## 编译和运行

```bash
# 运行程序（不生成文件）
go run day1.go

# 编译程序（生成可执行文件）
go build day1.go        # 生成 day1 或 day1.exe
./day1                  # 运行

# 格式化代码
go fmt day1.go

# 查看版本
go version

# 查看环境
go env
```

## 常见错误

### 1. 未使用的变量
```go
// ❌ 错误
func main() {
    x := 10  // 声明了但没使用
}

// ✅ 正确
func main() {
    x := 10
    fmt.Println(x)
}

// ✅ 或使用空白标识符
func main() {
    _ = 10  // 明确表示不使用
}
```

### 2. 短声明只能在函数内
```go
// ❌ 错误：在包级别使用 :=
package main
name := "张三"  // 编译错误

// ✅ 正确：包级别必须用 var
package main
var name = "张三"
```

### 3. 重复声明
```go
// ❌ 错误
func main() {
    x := 10
    x := 20  // 编译错误：重复声明
}

// ✅ 正确
func main() {
    x := 10
    x = 20   // 赋值，不是声明
}
```

### 4. 隐式类型转换
```go
// ❌ 错误
var i int = 10
var f float64 = i  // 编译错误

// ✅ 正确
var i int = 10
var f float64 = float64(i)
```

## 测试相关应用场景

### 场景1: 测试配置
```go
const (
    TestTimeout = 30        // 秒
    MaxRetries  = 3
    ApiVersion  = "v1"
)
```

### 场景2: 测试状态
```go
const (
    TestPending = iota + 1
    TestRunning
    TestPassed
    TestFailed
)
```

### 场景3: 测试统计
```go
totalCases := 100
passedCases := 85
passRate := float64(passedCases) / float64(totalCases) * 100
fmt.Printf("通过率: %.2f%%\n", passRate)
```

### 场景4: 环境配置
```go
const (
    DevURL  = "http://dev.example.com"
    TestURL = "http://test.example.com"
)

var currentEnv = "test"
var debugMode = true
```

## 💡 记忆技巧

1. **变量声明**：记住 `:=` 是最常用的（函数内）
2. **类型转换**：Go很严格，全部显式转换
3. **iota**：const块里的自动计数器，从0开始
4. **零值**：数字0、布尔false、字符串空
5. **格式化**：%d整数、%f浮点、%s字符串、%v万能

## 🎯 Day 1 核心要点

- ✅ 掌握三种变量声明方式
- ✅ 理解Go的类型系统和零值
- ✅ 必须显式类型转换
- ✅ 使用const和iota定义常量
- ✅ 熟练使用fmt包格式化输出
- ✅ 了解Go与Java的主要区别

---

**下一步**：完成Day 1的4个练习题，然后开始Day 2！🚀

